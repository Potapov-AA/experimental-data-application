from accessify import private
from matplotlib import pyplot as plt
import numpy as np
import math

class Processing():
    def __init__(self): pass
    
    def antiShift(data = [i for i in range(1000)]):
        fig, ax = plt.subplots(figsize=(10, 6))
        ax.scatter(x=[i for i in range(len(data))], y=data)

        plt.show()